#include<bits/stdc++.h>
using namespace std;

int main(){
    int n;
    string s;
    cin>>n;
    cin.ignore(); // otherwise it will take enter as input.
    getline(cin,s); // take string input with space
    cout<<n<<endl;
    cout<<s;
}